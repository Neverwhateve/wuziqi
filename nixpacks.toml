[[env]]
name = "NIXPACKS_NODEJS_VERSION"
value = "22"

[phases.setup]
nixpkgs = ["unstable"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build:client", "npm run copy:public"]

[phases.start]
cmds = ["npm start"]

[nodejs]
version = "22"